# Plan for Future Considerations: Internationalization.

## Objective
Identify all user-facing strings. Implement a simple i18n mechanism (e.g., using a JavaScript object for translations). Provide a language selector in settings. Translate key phrases into a few common languages.
